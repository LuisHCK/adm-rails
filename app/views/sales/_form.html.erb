<%= simple_form_for(sale) do |f| %>
  <% if sale.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
        <% sale.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>
      <div class="row">
        <div class="col-md-4 col-sm-6">
          <%= f.association :stock, label_method: :stock_name, value_method: :id, include_blank: false %>

        </div>
        <div class="col-md-4 col-sm-6">
          <%= f.input :quantity %>
        </div>
        <div class="col-md-4 col-sm-6">
          <%= f.input :notes %>
        </div>
      </div>

      <div class="actions">
        <%= f.submit class:"btn btn-success" %>
      </div>
      <% end %>